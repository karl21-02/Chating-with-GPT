server:
  port: 8010
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  mvc:
    view:
      prefix: /templates/
      suffix:
  profiles:
    include: API-KEY
openai:
  api-key: NEED_TO_INPUT_API
chatgpt:
  api-key: NEED_TP_INPUT_API
